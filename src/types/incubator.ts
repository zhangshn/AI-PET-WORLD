/**
 * ======================================================
 * AI-PET-WORLD
 * Incubator Type
 * ======================================================
 *
 * 当前文件负责：定义孵化器的数据结构
 *
 * 设计说明：
 * - 孵化器只负责“生命是否能顺利诞生”
 * - 不参与人格计算
 * - 不存储人格信息
 *
 * 后续可扩展：
 * - 多胚胎槽位
 * - 稀有度 / 基因质量
 * - 环境影响（温度 / 稳定场）
 * ======================================================
 */

/**
 * ======================================================
 * 孵化器状态
 * ======================================================
 */
export type IncubatorStatus =
  | "incubating"      // 孵化中
  | "ready_to_hatch"  // 已达到孵化条件
  | "hatched"         // 已孵化完成

/**
 * ======================================================
 * 孵化器结构
 * ======================================================
 */
export type IncubatorState = {
  /**
   * 是否存在胚胎
   */
  hasEmbryo: boolean

  /**
   * 胚胎名称（出生后宠物名称）
   */
  embryoName: string

  /**
   * 孵化进度（0 - 100）
   */
  progress: number

  /**
   * 稳定度（0 - 100）
   */
  stability: number

  /**
   * 当前状态
   */
  status: IncubatorStatus
}