// 该文件保留树木体积实验入口，当前回退为原始 SVG 输出。

import type { PixelTreeRenderTestResult } from "./tree-render-test-module";

export function addTreeVolumePreviewLayer(
  svg: string,
  test: PixelTreeRenderTestResult,
): string {
  void test;
  return svg;
}
