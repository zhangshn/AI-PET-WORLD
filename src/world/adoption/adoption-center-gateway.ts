/**
 * 当前文件负责：导出领养中心状态兼容入口。
 */

export { buildEmptyAdoptionState } from "./adoption-center-schema"

export type {
  AdoptionSource,
  AdoptionState,
  AdoptionStatus,
} from "./adoption-center-schema"
