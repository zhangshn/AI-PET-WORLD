# Live World Visual Input

Future home for building `ChunkVisualInput` from `ChunkState`.

The builder must not let AI Painter decide world facts. It only converts world facts into structured visual input.

