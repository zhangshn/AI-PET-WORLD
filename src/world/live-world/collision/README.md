# Live World Collision

Future home for collision projection:

```txt
TileState + WorldEntity.collision
-> collisionLayer
-> walkableLayer
-> interactionLayer
-> TileState.projectedBlocksMovement / projectedBlocksVision
```

P0 only defines the types.

