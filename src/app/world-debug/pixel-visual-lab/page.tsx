// 该页面用于承载合并后的像素视觉 Debug 实验室。

import PixelVisualLabClient from "./pixel-visual-lab-client";

export default function PixelVisualLabPage() {
  return <PixelVisualLabClient initialMode="composer" />;
}
