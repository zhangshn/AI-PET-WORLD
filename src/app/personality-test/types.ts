/**
 * 当前文件负责：定义 personality-test 页面局部使用的类型。
 */

import type { ZiweiFlowType } from "../../ai/ziwei-core/dynamic/dynamic-schema"

export type DynamicGenderInput = "" | "male" | "female"

export type ActiveDynamicFlow = ZiweiFlowType