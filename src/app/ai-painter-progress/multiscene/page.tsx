import Link from "next/link"
import styles from "../detail.module.css"

export default function MultisceneTrainingPage() {
  return <main className={styles.page}><header className={styles.header}><Link className={styles.back} href="/ai-painter-progress">← 返回训练主页</Link><p className={styles.kicker}>STAGE 02 / MULTI-SCENE</p><h1>多场景结构训练结果</h1><p>使用 12 张项目场景、14 通道 Blueprint 条件，对基础模型、GAN 和结构加权 V2 进行真实对照。</p><dl className={styles.metrics}><div><dt>训练/验证</dt><dd>10 / 2</dd></div><div><dt>结构 V2</dt><dd>180 Epoch</dd></div><div><dt>V2 GAN</dt><dd>200 Epoch</dd></div><div><dt>视觉审核</dt><dd>未通过</dd></div></dl></header><section className={styles.resultGrid}><article className={styles.resultCard}><span className={styles.fail}>审核未通过</span><h2>基础结构模型</h2><p>学到了森林、道路和建筑的大致位置，但输出严重平均化和模糊。</p><img src="/api/ai-painter/multiscene-inference/base" alt="多场景基础模型输出" /></article><article className={styles.resultCard}><span className={styles.fail}>审核未通过</span><h2>GAN 细节模型</h2><p>自然纹理变清晰，但建筑结构丢失，不能作为游戏画面。</p><img src="/api/ai-painter/multiscene-inference/gan" alt="多场景 GAN 模型输出" /></article><article className={styles.resultCard}><span className={styles.fail}>审核未通过</span><h2>结构加权 V2</h2><p>加入同步增强和建筑、道路、水岸加权损失后，纹理继续改善，但建筑与水域仍未被可靠还原。</p><img src="/api/ai-painter/multiscene-inference/structural-v2" alt="结构加权 V2 模型输出" /></article></section><section className={styles.panel}><h2>审核结论与下一步</h2><p>三轮结果均不会进入 `/world`。下一阶段停止单纯堆叠像素损失，改为显式结构监督：先让模型预测 14 通道结构，再以结构结果约束 RGB 生成，分别验证建筑、道路和水岸是否保真。</p></section></main>
}
