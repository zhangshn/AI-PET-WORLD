/**
 * Backward-compatible export bridge for legacy import paths.
 */

export {
  drawStaticWorld,
  getStaticWorldRenderKey,
  type DrawStaticWorldInput,
  type StaticWorldLayerRefs,
} from "./environment/stage-static-world-renderer"
