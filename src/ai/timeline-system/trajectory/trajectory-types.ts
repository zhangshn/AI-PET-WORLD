/**
 * AI-PET-WORLD
 * 时间线系统 - 生命轨迹层类型定义（第一版）
 *
 * ----------------------------------------------------------------------------
 * 【这个文件负责什么？】
 * ----------------------------------------------------------------------------
 * 这个文件专门负责定义 timeline-system 中“生命轨迹层”的核心类型。
 *
 * 它只定义：
 * - 时间线事件记录（TimelineEventRecord）
 * - 趋势方向（TrendDirection）
 * - 趋势结构（TrajectoryTrend）
 * - 分支标签（BranchTag）
 * - 生命轨迹总结构（LifeTrajectory）
 *
 * ----------------------------------------------------------------------------
 * 【这个文件不负责什么？】
 * ----------------------------------------------------------------------------
 * 这个文件不负责：
 * - 写入轨迹事件
 * - 计算分支
 * - 生成摘要
 * - 更新趋势
 * - 和状态、时序系统做实际联动
 *
 * 它只是“生命轨迹层的数据结构基础文件”。
 *
 * ----------------------------------------------------------------------------
 * 【生命轨迹层在系统中的位置】
 * ----------------------------------------------------------------------------
 * 先天人格固定存在
 * -> 时间推进
 * -> 世界事件进入
 * -> 在先天人格框架内引发状态变化
 * -> 状态推动行为
 * -> 事件与行为在时间线上留下痕迹
 * -> 痕迹逐渐累积成趋势
 * -> 趋势逐渐形成生命分支
 *
 * 所以轨迹层不是“当前状态”，
 * 而是“过去如何一步步塑造成现在”。
 */

/* -------------------------------------------------------------------------- */
/* 一、基础通用类型                                                             */
/* -------------------------------------------------------------------------- */

/**
 * 比例值
 *
 * 推荐语义：
 * - 一般用于 0 ~ 1 的归一化数值
 * - 例如：事件影响强度、趋势强度
 *
 * 当前先只做类型别名，不做运行时限制。
 */
export type RatioValue = number;

/* -------------------------------------------------------------------------- */
/* 二、时间线事件记录（Timeline Event Record）                                  */
/* -------------------------------------------------------------------------- */

/**
 * 时间线事件记录
 *
 * 用于把“对生命轨迹有意义的事件”记录下来。
 *
 * 注意：
 * 这里的记录，不是全项目所有事件的替代品。
 * 它更像是：
 * - 从世界事件里挑出会影响生命线演化的内容
 * - 再写进轨迹层
 *
 * 例如未来可能记录：
 * - 出生
 * - 被喂食
 * - 被照看
 * - 被忽视
 * - 行为明显变化
 * - 情绪明显变化
 * - 环境刺激
 * - 阶段性转折
 */
export interface TimelineEventRecord {
  /**
   * 事件唯一 ID
   *
   * 用于区分不同记录。
   * 未来可以由 recorder 统一生成。
   */
  id: string;

  /**
   * 事件发生时的 world tick
   *
   * 用于按时间线排序，也方便后续调试和回放。
   */
  tick: number;

  /**
   * 事件发生时的第几天
   */
  day: number;

  /**
   * 事件发生时的第几小时
   */
  hour: number;

  /**
   * 事件类型
   *
   * 当前先用 string，便于灵活扩展。
   *
   * 示例：
   * - "born"
   * - "fed"
   * - "rested"
   * - "comforted"
   * - "disturbed"
   * - "behavior_shift"
   * - "mood_shift"
   * - "bonding"
   */
  type: string;

  /**
   * 事件说明
   *
   * 用于：
   * - 调试
   * - 摘要生成
   * - 时间线回放
   * - world 页未来展示
   */
  message: string;

  /**
   * 事件影响强度，建议范围 0 ~ 1
   *
   * 含义：
   * - 数值越高，说明这个事件对生命轨迹影响越明显
   * - 例如轻微波动可用 0.1 ~ 0.2
   * - 重大转折可用 0.7 ~ 1
   *
   * 当前先作为预留字段，
   * 后续 recorder / branch-calculator 可以开始使用。
   */
  impact?: RatioValue;

  /**
   * 事件附加数据
   *
   * 当前先保持宽松结构，方便不同类型事件附带额外信息。
   *
   * 例如未来可能塞入：
   * - previousState
   * - nextState
   * - relatedAction
   * - relatedMood
   * - relatedPhase
   */
  payload?: Record<string, unknown>;
}

/* -------------------------------------------------------------------------- */
/* 三、轨迹趋势（Trajectory Trend）                                             */
/* -------------------------------------------------------------------------- */

/**
 * 趋势方向
 *
 * 用于表达某个长期趋势是在增强、稳定，还是下降。
 *
 * 注意：
 * 它描述的是“长期走向”，不是一时一刻的状态。
 */
export type TrendDirection =
  | "rising"   // 上升中
  | "stable"   // 稳定
  | "falling"; // 下降中

/**
 * 轨迹趋势结构
 *
 * 用于表达某个维度在生命时间线中的长期趋势。
 *
 * 它和当前状态的区别：
 * - 当前状态：描述“此刻”
 * - 轨迹趋势：描述“长期走向”
 *
 * 例如：
 * - trustTrend 可能是 rising
 * - stressTrend 可能是 falling
 * - explorationTrend 可能是 stable
 */
export interface TrajectoryTrend {
  /**
   * 趋势方向
   *
   * 例如：
   * - rising：这个维度在增强
   * - stable：这个维度目前整体平稳
   * - falling：这个维度在减弱
   */
  direction: TrendDirection;

  /**
   * 趋势强度，建议范围 0 ~ 1
   *
   * 含义：
   * - 越高：趋势越明显
   * - 越低：趋势越弱，甚至接近无明显长期倾向
   *
   * 例子：
   * - direction = rising, strength = 0.9
   *   表示这个趋势正非常明显地上升
   *
   * - direction = stable, strength = 0.1
   *   表示当前基本没有显著趋势波动
   */
  strength: RatioValue;
}

/* -------------------------------------------------------------------------- */
/* 四、生命分支标签（Branch Tag）                                               */
/* -------------------------------------------------------------------------- */

/**
 * 分支标签
 *
 * 用于表达：
 * 当前这条生命线，更接近哪一种长期发展路径。
 *
 * 注意：
 * - 这不是先天人格标签
 * - 这不是当前瞬时状态标签
 * - 这是“经历累积后形成的生命线方向”
 *
 * 它可以随着时间和事件积累逐渐变化。
 */
export type BranchTag =
  | "balanced"   // 相对平衡，尚未形成明显单一路径
  | "attachment" // 更偏亲近、依附、建立连接的发展路径
  | "defense"    // 更偏警觉、防御、回避刺激的发展路径
  | "curiosity"  // 更偏探索、接触世界、主动观察的发展路径
  | "recovery"   // 更偏修整、恢复、重新稳定的发展路径
  | "survival";  // 更偏应对压力、维持基本状态的路径

/* -------------------------------------------------------------------------- */
/* 五、生命轨迹总结构（Life Trajectory）                                        */
/* -------------------------------------------------------------------------- */

/**
 * 生命轨迹
 *
 * 这一层回答的问题是：
 * - 这只宠物一路经历了什么？
 * - 哪些倾向在长期累积？
 * - 当前生命时间线更偏向哪条分支？
 * - 为什么未来映射会和别的个体不同？
 *
 * 它和状态层的区别非常重要：
 *
 * - 状态层（PetState）
 *   描述的是：此刻状态怎样
 *
 * - 轨迹层（LifeTrajectory）
 *   描述的是：一路如何走到现在
 *
 * 所以轨迹层是“时间沉淀的结果”。
 */
export interface LifeTrajectory {
  /**
   * 已记录的重要时间线事件
   *
   * 当前第一版先直接用数组。
   *
   * 后续如果事件变多，可以考虑拆成：
   * - recentHistory：最近事件
   * - milestoneHistory：关键里程碑事件
   * - counters：聚合统计
   */
  history: TimelineEventRecord[];

  /**
   * 信任趋势
   *
   * 长期看：
   * - 是越来越容易信任环境/照顾者
   * - 还是信任在下降
   */
  trustTrend: TrajectoryTrend;

  /**
   * 压力趋势
   *
   * 长期看：
   * - 是越来越紧绷
   * - 还是越来越放松
   *
   * 注意：
   * 这里记录的是“长期压力走势”，
   * 不是某一刻 stress 数值。
   */
  stressTrend: TrajectoryTrend;

  /**
   * 探索趋势
   *
   * 长期看：
   * - 是越来越愿意探索
   * - 还是越来越收缩和停滞
   */
  explorationTrend: TrajectoryTrend;

  /**
   * 依附趋势
   *
   * 长期看：
   * - 是否越来越靠近熟悉对象
   * - 是否越来越形成依附模式
   */
  attachmentTrend: TrajectoryTrend;

  /**
   * 回避趋势
   *
   * 长期看：
   * - 是否越来越倾向躲避刺激
   * - 是否越来越偏向防御和退缩
   */
  avoidanceTrend: TrajectoryTrend;

  /**
   * 当前生命分支标签
   *
   * 这是对当前生命时间线整体方向的抽象总结。
   *
   * 例如：
   * - balanced
   * - attachment
   * - defense
   * - curiosity
   *
   * 注意：
   * 这个标签不是永恒不变的，
   * 后续可以随着长期积累而变化。
   */
  branchTag: BranchTag;

  /**
   * 当前阶段摘要
   *
   * 用于：
   * - world 页面
   * - 调试页
   * - 时间线摘要展示
   *
   * 示例：
   * - "逐渐形成对照顾者的依附倾向"
   * - "近期更偏向谨慎观察与回避刺激"
   * - "正在从恢复阶段过渡到探索阶段"
   *
   * 当前第一版先保留为 string。
   */
  summary: string;
}