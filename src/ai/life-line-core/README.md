# life-line-core

当前目录属于贯穿 10 层架构的 Life Line 主轴，不是独立编号层。

当前负责：预留生命线主轴的上下文、编排和跨层生命过程说明。未来它会把命理底色、人格、意识、日常状态、记忆、学习、驱动、行为、世界反馈和展示结果连接为连续生命线。

当前不能做：不能直接写命理算法，不能直接控制宠物或管家行为，不能替代 memory、learning、drive、goal、behavior 或 world engine。

后续扩展方向：新增 Life Line context、生命阶段汇总、跨层只读快照、迁移校验和 gateway。
