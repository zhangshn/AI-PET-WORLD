# World Visual Candidates 候选目录

AI Painter 输出必须先进入候选目录。

```txt
data/world-visual-candidates/{candidateId}/
  input.chunk.json
  output.image.png
  candidate.meta.json
```

候选图不等于训练样本。未经人工复核，不得进入 `data/world-samples/`。

