# 紫微斗数目录结构

更新时间：2026-08-03 09:23:45 +08:00

状态：active-subsystem-directory-reference

Codex等外部执行智能体不得超出当前用户任务范围；本地程序在生效业务、安全和机器合同内自主运行，不从聊天或本句推导逐步Owner审批。

## 文档层

`docs/ziwei/`只保存长期架构、字段、算法、来源、页面验收和稳定复核合同，不保存路线图、执行表、阶段进度或当前下一动作。

## 代码层

| 路径 | 职责 |
|---|---|
| `src/ai/destiny-core/` | 紫微斗数计算、内容字典和人格输入转换 |
| `src/app/api/ziwei/` | 只通过正式合同暴露数据与分析结果 |
| `src/app/ziwei/` | 页面展示，不直接改写底层算法 |
| `scripts/ziwei/` | 数据、来源、回归和验收检查 |
| `data/ziwei/` | 黄金样本、校准和机器数据 |

## 数据边界

公版资料、现代资料元信息、项目自有摘要、算法规则、复核样本和页面输出必须分别标识来源、许可、版本与用途。紫微结果只能通过人格映射合同服务AI管家，不能直接写成角色行动或世界事实。
