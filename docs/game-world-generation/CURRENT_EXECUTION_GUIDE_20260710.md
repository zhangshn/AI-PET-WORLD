# AI-PET-WORLD 唯一模块计划表

更新时间：2026-09-20 23:37:46 +08:00

状态：active-module-plan / AI Painter固定进度3/5（60%）仅为既有地图候选链里程碑；当前任务及活动执行以机器登记为准；完整产品MVP尚未完成

Codex等外部执行智能体不得超出当前用户任务范围；本地程序在生效业务、安全和机器合同内自主运行，不从聊天或本句推导逐步Owner审批。

本文是全项目唯一模块计划表，只记录模块级目标、边界、验收条件和阶段状态，包含依赖、阻断和最近一次模块终态，不保存逐命令修复清单、Run／Epoch／hash流水。动态状态以唯一当前执行登记及其事务、原始证据和SQLite为准；本文不是实时监控数据库。

表中状态不构成聊天授权。Owner职责只采用权威索引的`GOV-OWNER-001`。本地AI在生效合同内自主推进，外部Codex只在当前委托范围内工作；缺数据、缺证据或程序错误不能转成逐阶段人工审批。

## 1. 当前模块计划

| 顺序 | 模块 | 目标与边界 | 当前状态／阻断 | 完成条件与后续准入 |
|---:|---|---|---|---|
| 1 | 平台可靠性与文档治理修复 | 统一业务、职责、状态、来源、历史隔离和完整MVP验收 | 正式目标已统一；文档检查器版本基线已同步并通过原检查入口；程序符合仍须按主体规格第17.3节逐项证明 | 权威链唯一、文档语义与链接正确；正式检查器与新版本对应；不得将文档通过等同模型通过 |
| 2 | AI Painter R5／Stage4静态自然地图 | 从WorldFacts和23通道生成原生完整RGB；保持64份48/8/4/4边界 | 既有候选链固定进度3/5（60%）；V1失败只读；V2保留原CPU记录；未激活后继split包及真实CPU取数、train优化步边界与批执行防护已建立；历史暴露／近邻审核、新能力与Smoke接入、单阶段Trainer和父权重加载未闭环；机器登记未切换 | 先关闭数据用途、历史暴露与近邻审核、状态映射和正式父权重传递缺口；精确新能力身份的CPU／GPU资格、Smoke、Stage0→2全部成功，再进入独立严格复验 |
| 3 | 本地自研AI MVP能力迁移 | 先将既定检查、执行、审核、裁决及终态流程迁移为不依赖Codex的本地闭环；当前先收口256×192有界路线，不建设通用代码模型或世界建设NPC | 已有登记、部分CPU生命周期与隔离进程观测原型；真实输入准入、生产执行接入、已知故障安全恢复与离线端到端验证仍未完成 | 验证启动端退出后执行器继续推进、失败不越门、同包不重复执行、证据与SQLite一致；未知锁保留且不自动重训。测试适配器、实验训练、正式Stage资格分别验收；正式阶段仍须模块2全部前置条件，不能以后台存活或256实验完成增加固定百分比 |
| 4 | 人物人格与初始外貌 | 紫微优先、缺时辰八字三柱；人格与外貌分离，映射模式与性别呈现分离 | 人格／八字已有代码基础；外貌规则数据和完整视觉链待建设；不把《麻衣神相》候选资料写成有效规则 | 主体规格MVP-VIS-02通过，映射来源／规则／参数／精度可追溯；稳定档案、合法降级与反例齐全 |
| 5 | 五类现实动物与生命数据 | 首个泰国区域的有限真实生物集及多态／健康／活动规则 | 五类的具体分类与代表物种尚待数据研究和版本化名录；现有植物目录不能代替 | MVP-VIS-03通过；五个类型逐一有合格物种／生境／状态；数据来源、权利、split与充足度审计通过 |
| 6 | 生命视觉与动态Runtime | 人物、动物身份稳定；Runtime事实驱动形态／健康／行动表现 | 新生命条件、模型责任、时序数据与审核尚待实现；不能占用现有自然物通道或直接改旧训练包 | 依赖模块4、5合格档案及模块2视觉基础；以独立能力版本完成MVP-VIS-04、06、07及模型全资格链 |
| 7 | 正式世界接入与整体MVP验收 | 自主感知→目标→行动→世界变化→记忆反馈，正式RuntimeFrame与保存恢复 | 既有Runtime／管家代码需完整集成验证；VJ-2、真实语义审核及正式发布不能由像素代理替代 | MVP-VIS-01至08全部取得真实证据；完整审核与机器发布通过；不依赖Codex窗口，不用静态图宣称完整MVP完成 |
| 8 | 控制台真实训练观察接入 | 按AI-CONSOLE-2.8/V23接入全部训练历史的独立观察索引与逐来源覆盖，提供原图、输出、完整分页指标/日志/审核和Checkpoint元数据 | 原登记历史及endpoint详情已接入；独立历史索引、旧格式适配、大内容分页与全覆盖核验仍待实现和验收，不标记完成 | 独立历史不被当前登记断链隐藏；旧/新数据可逐项查询，未记录/缺失/冲突明确；新增记录自动出现，分页完整，当前态与资源不退化；不修改训练数据、资格或原始证据 |

表中顺序表示依赖与验收关系；模块4、5的数据研究可在合规范围并行，不能阻塞不依赖生命数据的自然地图诊断，也不能绕过模块2、3的机器门。模块状态变化才更新本表。

## 2. AI Painter固定五阶段与完整MVP的区别

固定五项是既有自然地图候选链的资格里程碑，不是完整MVP百分比，也不能换算剩余时间：

1. 失败证据与修复方向：已有完成记录。
2. 候选、训练器支持与隔离配置：已有完成记录；后继版本仍须自己的合同与行为测试。
3. 固定GPU资格：既有候选链保留历史完成记录；不能迁移成V2已通过。V2必须重新形成256×192、512×384、1024×768的真实GPU证据。
4. Stage0→Stage1→Stage2完整训练、固定复现与机器审核：未完成。全数据筛查结束或CPU通过均不计入正式阶段成功。
5. 不参与权重更新和Checkpoint选择的独立严格复验：未完成。

因此“固定进度3/5（60%）”只描述该继承计数；它不证明V2完成三项资格，更不表示人物、动物与整个产品完成60%。新生命能力必须独立登记资格，不沿用这个百分比。

## 3. 当前实现与资产边界

当前执行优先级是解除本地AI Painter正式Stage4训练链的实际阻断，完成模块2声明的资格与训练里程碑。256×192实验与能力迁移只推进对此有直接作用的有界工作；测试数量、诊断完成和外部工具产图均不计入固定阶段进度。外部RGB生成与局部修图支线暂停，已有候选及失败证据原样保留，不进入默认训练选择。诊断优先使用精确绑定的既有原图、条件、本地模型输出及执行证据，区分数据矛盾、模型表示、训练目标和执行问题；暂停外部修图不等于旧数据自动合格，已知拒绝、来源与语义缺口仍按原准入规则处理。只有证据支持的最小修复形成新版本并完成对应CPU／GPU准入后，才执行有上限的本地训练；不得重放已结束的实验、降低门槛或把外部修好的图片当作本地模型能力提升。

当前自然地图模块增加一个先行、有界的256×192学习能力实验，按主体规格第9.6节及独立实验机器政策执行：只取少量既有train原图，复用V2模型／Loss与精确冻结基础资产，验证真实短训练、Checkpoint重载和推理。它不要求先宣告整批64份正式合格，也不改变64及48/8/4/4；不触及人物、动物、Runtime发布或更高分辨率。实验结果只记录在机器证据，完成不增加固定Stage4百分比；正式资格路线及已有阻断继续保留。未通过实验使用边界、CPU反例或资源检查时禁止GPU执行。

当前V2是独立后继能力，不是原地修改V1。V1失败Denoiser、Checkpoint、运行目录和旧训练入口只作历史复核，不得恢复默认调度、加载失败权重或自动重试。

V2显式绑定的64份旧来源数据和项目基础Autoencoder是不同角色的合法候选依赖；只有重算来源、SHA、结构、冻结状态及用途后才能使用。“有旧日期／V7字段”不等于垃圾或旧模型仍在运行。具体保留、替代、停用按数据规格第4.2与13.1节执行，不进行顺带清理。

四段生成责任与Stage0／1／2分辨率阶段分开；人物动物须新条件扩展和新能力身份，不原地改现有23通道。参数数量或单模型／共享底座／隔离组件不是永久业务目标。

## 4. 最近一次模块终态

V1在正式Stage0之前的全数据筛查中以真实视觉失败关闭，不能将“筛查完成”记为正式阶段完成或能力发布。当前V2已有CPU合同验收记录，但没有据此获得GPU资格、Smoke、正式训练或发布成功。

当前登记与完整规范仍有适配差距，详见主体规格第17.3节。历史检查或本表旧结论不能替代本次资格链的真实输入、行为与输出证据；不在Markdown复制终态hash或逐次运行日志。

## 5. 当前阻断与唯一下一动作

当前任务、活动执行和下一动作只从经验证的唯一机器登记读取，不在本文固定为某个历史规划入口。有界256×192实验终态不能替代正式V2下一动作；没有合格后继包时不得重放旧实验或扫描历史目录选取任务。文档基线和执行器代码已开始适配，但不能据此改写机器资格、消费未满足前置条件的动作，或把“存在规划脚本”写成“编排器必然已经持续推进”。

进入相应执行之前必须按依赖先满足：

1. 精确核验当前能力、程序血缘、父资产、CPU终态与结构化下一动作；处理CPU枚举与失败阶段语义差距。
2. 后继四split文件及实际Dataset选择已有CPU证据；补齐历史暴露／变换近邻审核和新能力绑定，将train优化步边界接入隔离Smoke并验证真实行为。原Smoke不因辅助组件测试通过而自动合格，不得用隔离诊断名义训练validation。
3. 正式批执行器保持无历史默认入口、同链父产物字节校验和失败关闭；在已建立的CPU文件级回归之外，补齐合格单阶段执行包、已登记Trainer、票据／资源监督及真实父Checkpoint加载验证。未登记合格入口时阻断，不使用旧入口补位。
4. 由本地编排器消费合法下一动作，依次完成只读GPU资格、受控Smoke、合同声明的筛查、正式Stage0→2；任一步失败保存事实并有界裁决。
5. 三个正式阶段全部通过才可将固定阶段4计为完成；随后独立严格复验、能力发布、Runtime接入按后续模块准入执行。

改变Smoke样本用途、数据发布或程序血缘须物化隔离的能力／执行身份并重新取得相应资格，不原地改写冻结合同或继承旧成功状态。以上是实施依赖，不是给外部智能体的常驻修改权限，也不是给本地AI增加人工审批。

## 6. 完整MVP完成条件

- 业务规格中的两大核心与反馈闭环均有真实运行证据。
- 自然世界沿用东南亚兼容档案及泰国首区事实包，不擅自扩展地区。
- 人物人格、初始外貌、五类动物、生命多态、行动与世界反馈、存档恢复全部通过主体规格第18节验收。
- 真实语义审核、模型能力发布和RuntimeFrame门全部通过；无已发布能力时保持正式展示关闭。
- 规范、机器合同、实现、测试与运行事实分别记录。文档完成不等于MVP完成，CPU检查不等于训练成功。
- 不因文档整编删除旧JSON、hash或失败证据；不降低审核门，不启用历史回退，不虚报剩余时间。

长期业务与技术边界见[业务规格](../BUSINESS_SPEC.md)、[总体架构](../ARCHITECTURE.md)、[本地AI能力与迁移架构](../LOCAL_SELF_DEVELOPED_AI_CAPABILITY_AND_CODEX_MIGRATION_ARCHITECTURE.md)和[AI Painter正式主体规格](AI_PAINTER_FORMAL_IMPLEMENTATION_SPEC.md)。
