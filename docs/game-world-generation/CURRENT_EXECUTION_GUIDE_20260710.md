# AI-PET-WORLD 唯一模块计划表

更新时间：2026-08-31 02:12:13 +08:00

状态：active-module-plan / AI Painter固定进度3/5（60%）；联合条件局部传输V1已以真实视觉失败关闭并退出当前路线；Stage4 V2 CPU合同验收已通过，当前无活动训练，唯一当前任务为只读GPU资格规划

Codex等外部执行智能体不得超出当前用户任务范围；本地程序在生效业务、安全和机器合同内自主运行，不从聊天或本句推导逐步Owner审批。证据冲突或政策边界必须失败关闭、保存事实并给出安全替代，不进入Owner等待状态。

本文档是项目唯一计划表，只记录模块级目标、边界、验收条件和阶段状态，并在当前模块内保留最近一次模块终态、当前阻断和唯一下一动作。一次命令、单次Run、哈希、内部票据或历史授权、消费记录、逐候选历史和训练流水不得写入Markdown；这些事实由本地程序保存到`data/`、`.runtime/`和SQLite。

表中状态不构成聊天授权。Owner职责只由`DOCUMENT_AUTHORITY_INDEX.md`中的`GOV-OWNER-001`定义；本地自研AI在生效项目合同内自主完成能力变更、训练、验证、审核、发布与回退，不得生成等待Owner批准的正常状态。Codex等外部执行者仍受Owner当前任务范围约束。同一执行包内的固定预览复现、validation、机器审核、只读分析、失败关闭、终态记录、监控和治理同步不得拆成人工操作。

## 1. 当前模块计划

| 顺序 | 模块 | 目标与边界 | 当前状态 | 验收与后续准入 |
|---:|---|---|---|---|
| 1 | 平台可靠性与文档治理修复 | 建立自主能力生命周期、内部任务票据、训练互斥、后台执行、状态投影和正式文档职责 | `GOV-OWNER-001`、三层状态机、可信发布、防重放、37条顶层需求追踪、历史合同边界和本地文档规则已建立；正式文档基线已区分文档生效与程序符合；唯一当前执行登记已按事务、SQLite提交记录和绑定SHA-256落盘，控制台只从该登记投影当前任务及最近训练终态，旧Smoke已从默认记录源移除并登记为历史只读 | 当前身份隔离正反回归、控制台回归、TypeScript、文档权威/状态机/需求追踪语义检查已通过；该结论不表示AI Painter程序全量符合或模型通过。登记无效时必须显示`unknown_or_stale`且禁止历史回退；后续生命周期转换必须复用同一登记协议 |
| 2 | AI Painter R5 / Stage4 | 从WorldFacts、VisualFactManifest和23通道条件生成可审核完整地图；不以失败预览或审核结果作为训练目标 | 固定进度3/5（60%）；V1真实视觉失败保持历史只读；独立V2能力已完成23通道类型采样、逐责任语义通路、最终RGB责任、Autoencoder自冻结、Dataset/Loss/审核/基础资产绑定和CPU合同验收；当前为`cpu_supported_inactive`，GPU资格、Smoke和Stage 0均未执行 | 只允许从当前V2父合同和当前执行登记建立只读GPU资格；禁止读取V1失败Checkpoint、扫描历史目录选候选、降低阈值或把CPU通过解释为模型通过 |
| 3 | 本地自研AI MVP能力迁移 | 能力设计、训练/生成、验证、审核、确定性裁决、发布、终态、治理记录和监控由本地程序闭环，Codex不成为Runtime依赖 | V1失败终态已由本地生命周期程序自动形成只读责任边界裁决，原子登记独立V2能力并完成CPU验收；来源`latestTrainingTerminal`保持不变，当前任务、后继动作、任务胶囊、事件账本和SQLite已同步 | 当前失败→裁决→能力变更→CPU验收链已闭环；后续只读GPU、Smoke、训练、自动验证和审核仍须沿同一登记协议逐项形成运行证据，不得恢复人工握手或目录扫描 |
| 4 | 世界生成与自主角色MVP接入 | 将通过严格机器验证并发布的视觉能力接入受控Runtime | 未开始 | 第一版能力通过完整机器发布门；后续RuntimeFrame按正式机器门自主运行 |

## 2. AI Painter固定五阶段

这五项是候选资格阶段，不是CPU检查次数、修复次数或训练轮数。只有完整阶段达到验收条件，固定总进度才前进一格。

1. 失败证据与修复方向：已完成。
2. 候选、训练器支持与隔离配置：已完成。
3. 固定GPU资格：已完成。
4. Stage 0→Stage 1→Stage 2完整训练、固定复现与机器审核：未完成；当前联合条件局部传输候选在Stage 0前的全数据筛查中真实视觉失败，Stage 0/1/2均未启动。
5. 不参与权重更新和Checkpoint选择的独立严格复验：未开始。

## 3. 当前实现边界

AI Painter对外只提供一个完整世界视觉入口。长期四段责任顺序由正式主体规格定义；具体能力版本可以使用单模型、共享底座或隔离组件实现，但必须登记责任映射并分别提供可验证的责任输出、证据和审核边界。任何具体模型、组件数量、参数组织和训练范式都只是可替换研发实现，不得写成永久业务架构。

联合条件局部传输V1是已被拒绝的未发布研发候选。其模型身份、代码血缘、运行终态、预览、审核、Checkpoint和输出目录全部保持历史只读，只允许作为裁决输入和回归对照；现行入口、模型工厂默认解析、训练程序和Codex均不得将它重新视为可执行候选。

当前实施是一个全新、隔离、未训练的后继能力变更，不是对V1的原地修改。后继能力必须同时收口：按类型重采样23通道；道路和四类对象的可验证语义通路；最终RGB对前序责任的显式消费；模型自保证Autoencoder冻结；完整Loss、审核阈值、Dataset Manifest/Source Index和基础资产绑定；训练目标与正式审核的逐责任对齐。任一项未通过则不得启动GPU。

## 4. 最近一次模块终态

联合条件局部传输V1全数据筛查已自然完成，训练输出完成固定字节复现和正式机器审核；专业画面审核通过，但道路与四类对象的条件语义持续失败，晚期节点没有形成稳定收敛，最终结果为真实视觉失败。该失败终态、审核和Checkpoint身份保持历史只读，不能晋级、读取权重、复用或重新调度。

本地生命周期程序已在不启动GPU和训练的情况下完成失败责任边界裁决：V1不满足条件类型采样、逐责任语义通路、最终RGB责任和模型自冻结要求，必须由独立能力身份替代。Stage4 V2父合同、模型/Trainer支持、64份数据发布、正式Loss、审核阈值、项目基础Autoencoder谱系及正反CPU测试已通过CPU验收；该终态只证明V2具备进入只读GPU资格规划的合同条件，不证明GPU可执行、Smoke合格、Stage 0成功或能力发布。

## 5. 当前阻断与后续实施顺序

模型CPU实现、机器合同和失败生命周期断点已经收口；当前不存在活动训练。唯一剩余实施顺序为：

1. 本地编排器从当前V2父合同、CPU验收终态和当前登记物化一次只读GPU资格包，验证真实CUDA前向、正式责任输出、梯度可达、显存边界和模型/基础Autoencoder状态不变；不得创建优化器、执行权重更新或读取失败Denoiser Checkpoint。
2. 只读GPU资格通过后，在同一能力身份下物化单个受控Smoke闭环；训练、固定预览、自动验证、机器审核、失败裁决和终态记录必须位于同一执行包，不产生Owner等待状态。
3. Smoke通过后，才可物化Stage 0→Stage 1→Stage 2正式训练闭环；任何阶段失败均保存事实并由本地程序自动裁决，不得原样自动重跑、增加Epoch、降低阈值或扩展多个未登记候选。
4. Stage 0、Stage 1和Stage 2全部达到冻结验收后，固定进度才从3/5（60%）更新为4/5（80%）。

## 6. 完成条件与固定边界

- 当前固定进度只能报告3/5（60%），不得用CPU测试、只读GPU诊断、文档或工具建设冒充Stage4训练完成。
- 本地自主闭环建设属于本地AI能力迁移，不增加AI Painter固定五阶段进度；它完成后减少重复人工操作和Codex在线依赖。
- “当前入口脚本白名单通过”与“当前执行身份隔离通过”是两个独立验收项；后者已由唯一登记、事务一致性、绑定证据重算、篡改失败关闭、重复初始化拒绝和历史隔离回归证明。后续程序不得将两者重新合并为目录扫描逻辑。
- 任何受控Smoke只有在其冻结合同要求的预览复现、机器审核、Manifest、Finalization和唯一终态全部形成后才算闭环。
- Smoke通过只获得Stage 0候选资格，不自动证明Stage 0成功；Smoke失败必须保存证据并按该次冻结裁决合同分类，只有裁决确认路线失败时才能退出路线、形成一个有界修复或进入政策边界选择。
- Stage 1只能加载同一合法路线的Stage 0成功Checkpoint；Stage 2只能加载同一路线的Stage 1成功Checkpoint。
- 失败、退出、Smoke、诊断或历史Checkpoint不得作为正式阶段初始化或晋级来源。
- 不得降低机器审核阈值、把失败预览像素或审核结果作为训练目标，也不得用部分产物补写成功终态。
- Stage5、正式推理、Checkpoint正式晋级、能力版本发布、RuntimeFrame和进入`/world`不属于当前执行范围。

长期业务与技术边界分别见[业务规格](../BUSINESS_SPEC.md)、[总体架构](../ARCHITECTURE.md)、[本地自研AI能力与迁移架构](../LOCAL_SELF_DEVELOPED_AI_CAPABILITY_AND_CODEX_MIGRATION_ARCHITECTURE.md)和[AI Painter正式主体规格](AI_PAINTER_FORMAL_IMPLEMENTATION_SPEC.md)。
