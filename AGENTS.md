# AI-PET-WORLD 智能体执行规则

更新时间：2026-08-24 12:43:09 +08:00

状态：active-agent-governance

Codex等外部执行智能体不得超出当前用户任务范围；本地程序在生效业务、安全和机器合同内自主运行，不从聊天或本句推导逐步Owner审批。

## 1. 必读顺序

1. `docs/DOCUMENT_AUTHORITY_INDEX.md`
2. `docs/BUSINESS_SPEC.md`
3. `docs/ARCHITECTURE.md`
4. `docs/LOCAL_SELF_DEVELOPED_AI_CAPABILITY_AND_CODEX_MIGRATION_ARCHITECTURE.md`
5. `docs/game-world-generation/CURRENT_EXECUTION_GUIDE_20260710.md`
6. 当前模块对应的一份正式规格

视觉字典只读取README、当前机器导出和任务明确涉及的条目，不默认全量载入。

## 2. 文档职责

- README只负责项目导航。
- 正式文档只定义长期业务、架构、合同、边界和验收标准。
- 全项目只保留一张模块计划表，由当前执行指南承担。
- 计划表只在模块建立、完成、失败关闭或范围实质变化时更新，不记录每次命令。
- 每次运行的授权、日志、Token、Checkpoint、审核和失败证据保存到`data/`、`.runtime/`和SQLite，不复制进Markdown。
- AGENTS不得保存执行历史、授权账本摘要或训练流水账。

## 3. 自主运行、安全与外部执行边界

- 本地自研AI系统以当前生效的业务、架构、数据、审核、资源和Runtime合同作为常驻运行边界。在该边界内，它可以自行规划、修改内部实现、训练或重训、验证、机器审核、选择Checkpoint、推进Stage、发布或回退能力版本、生成RuntimeFrame、进入世界并保存证据，不需要逐任务、逐阶段、逐版本或逐次运行的Owner签名与批准。
- 本地内部任务票据只用于幂等、防重、状态转换、资源配额和证据追溯，不是人工授权，也不得被设计成等待Owner的握手券。
- 本地系统不得自行改变长期业务目标、WorldFacts权威边界、来源许可规则、安全上限或审计真实性；不得为通过而降低审核阈值、删除失败证据、使用未获许可的外部数据、产生未登记外部费用，或执行不可恢复的破坏性操作。
- 触及上述项目边界时必须失败关闭并生成政策边界报告。Owner职责只以`docs/DOCUMENT_AUTHORITY_INDEX.md`中的`GOV-OWNER-001`为准；Owner可以主动修改业务目标、暂停或紧急停止，但不是本地AI日常训练、验证、发布或世界运行的审批员。
- 能力发布身份必须来自本地程序对真实不可变数据、模型、审核、Runtime、条件和测试证据的重新计算与原子登记；不得相信调用方布尔字段、聊天、历史合同或格式正确的SHA字符串。
- Codex及其他外部智能体不继承本地自研AI的常驻运行权。外部智能体只能在Owner当前明确任务范围内修改项目；不得从旧“允许”“继续”、历史计划或本地AI内部票据推导新的外部写操作权限。
- CPU只读检查、配置审计、确定性裁决、监控、控制台投影和治理记录属于本地系统日常职责，不应被拆成Owner逐步授权。
- 破坏性动作必须核对绝对目标和范围；不得删除运行证据来掩盖失败。

## 4. 实施规则

- 修改前检查工作区，保留用户已有和无关改动。
- 使用正式目录和既有架构，不建立平行后台、阶段文档树或历史文档目录。
- 代码修改后运行与风险相称的类型、构建、测试、安全和文档检查。
- 失败必须输出具体错误、证据路径和影响范围；不得降低门槛伪造通过。
- 本地程序和机器证据是正式系统记录；聊天与外部智能体记忆不是。

## 5. 本地自研AI与Codex边界

- 当前MVP由本地自研AI系统展现人物自主、世界生成、完整地图、动态Runtime和人物—世界闭环。
- 本地自研AI训练系统由本地模型以及本地训练、验证、审核和证据程序组成。
- Codex当前按Owner命令负责代码建设、编译测试、复杂诊断和有界修复；这些能力以后逐项迁移。
- Codex不得成为授权机关、系统记录或Runtime必要依赖；目标角色是只读监控、证据核验和技术建议。
- 本文件中的外部任务范围约束只适用于Codex和其他外部文档/代码执行智能体，不能被程序解释为本地自研AI运行状态机的逐步人工审批要求。
